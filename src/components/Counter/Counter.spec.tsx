import { render, screen } from '@testing-library/react'

// import { Counter } from '.'

describe('Counter component', () => {
  it.todo('should render with success')
  it.todo('should decrement -1 on counter when on click decrement button')
  it.todo('should increment +1 on counter when on click increment button')
})
